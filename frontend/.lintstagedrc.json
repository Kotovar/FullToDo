{
  "src/**/*.{js,ts,tsx}": [
    "prettier --write",
    "eslint --ignore-pattern .gitignore --fix",
    "tsc --noEmit",
    "vitest related --run --environment=jsdom"
  ],
  "src/**/*.{ts,tsx}": ["npm run fsd:check"],
  "*.{json,md}": ["prettier --write"]
}
