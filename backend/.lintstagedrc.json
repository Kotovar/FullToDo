{
  "*.{js,ts}": [
    "prettier --write",
    "eslint --ignore-pattern .gitignore --fix",
    "bash -c tsc --noEmit",
    "vitest related --run --environment=node"
  ],
  "*.{json,md}": ["prettier --write"]
}
