DB_TYPE=mock # mongo / postgres / mock
SERVER_TYPE=http # express / http / nextJs
PORT=5000
VITE_URL=http://localhost:${PORT}
NODE_ENV=development

DB_USER='postgres'
DB_PASSWORD='secret'
DB_HOST='localhost'
DB_PORT=5432
DB_NAME='fulltodo'
